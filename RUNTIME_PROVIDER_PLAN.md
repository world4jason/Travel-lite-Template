# Runtime provider milestone

This milestone adds static-host-friendly runtime integrations for weather, place search/enrichment, interactive maps, and Google Maps handoff while preserving `trip.json` as the shared source of truth and IndexedDB as the local mutable store.
